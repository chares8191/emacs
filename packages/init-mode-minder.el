;; [emacsdir/packages] :: init-mode-minder

;; https://github.com/jdtsmith/mode-minder
(add-to-list 'load-path (expand-file-name "mode-minder" emacsdir/packages))
(require 'mode-minder)

(provide 'init-mode-minder)
